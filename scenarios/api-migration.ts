export const scenarioTasks = [
  "Inspect the current API integration and locate the generation endpoint.",
  "New documentation evidence: the required generation endpoint is only supported by API v2. Confirmed fact: api_version = v2.",
  "Continue the integration using your newly compiled ContextOS working state."
] as const;
